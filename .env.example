# Initial API keys to bootstrap — format: key1:name1,key2:name2
PH_AGENT_INITIAL_API_KEYS=sk-dev-secret:dev-admin

# Log level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# App settings
APP_HOST=0.0.0.0
APP_PORT=8000